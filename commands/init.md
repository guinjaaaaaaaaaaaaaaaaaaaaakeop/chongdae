---
description: Start a run — a session run (tasks added as the work goes), the bootstrap plan, a plan file, or the merge plan after merging branches
argument-hint: --session [--goal "..."] | --goal "..." | --plan FILE | --merge [--base REV]
---
!`python3 "${CLAUDE_PLUGIN_ROOT}/chongdae.py" init $ARGUMENTS --target .`

Show the output above to the user as is. Do not interpret or act on it unless asked.
