---
description: Accept decisions a provider made beyond the contract (after writing them into the plan)
argument-hint: <task> --by NAME | --delegated WHY
---
!`python3 "${CLAUDE_PLUGIN_ROOT}/chongdae.py" accept $ARGUMENTS --target .`

Show the output above to the user as is. Do not interpret or act on it unless asked.
