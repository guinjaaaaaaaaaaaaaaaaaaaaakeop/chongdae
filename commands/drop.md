---
description: A session task that will not be done — leaves the open set with its reason, stays in the record
argument-hint: <id> --why ".." [--by NAME]
---
!`python3 "${CLAUDE_PLUGIN_ROOT}/chongdae.py" drop $ARGUMENTS --target .`

Show the output above to the user as is. Do not interpret or act on it unless asked.
