---
description: Take a task (default: git user.name); run skips tasks claimed by someone else
argument-hint: <id> [--by NAME]
---
!`python3 "${CLAUDE_PLUGIN_ROOT}/chongdae.py" claim $ARGUMENTS --target .`

Show the output above to the user as is. Do not interpret or act on it unless asked.
