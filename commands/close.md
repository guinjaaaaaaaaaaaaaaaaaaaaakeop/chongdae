---
description: End the session run (open tasks stay open); a worktree run merges back — a completed plan run too
argument-hint: [--run <id>]
---
!`python3 "${CLAUDE_PLUGIN_ROOT}/chongdae.py" close $ARGUMENTS --target .`

Show the output above to the user as is. Do not interpret or act on it unless asked.
